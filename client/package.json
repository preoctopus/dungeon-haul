{
  "name": "@dhaul/client",
  "version": "0.1.0",
  "private": true,
  "description": "Dungeon Haul client — Phaser 3 + Vite. P0: boot clear-color scene at 960x540 only.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
