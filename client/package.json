{
  "name": "@dhaul/client",
  "version": "0.1.0",
  "private": true,
  "description": "Dungeon Haul client — Phaser 3 + Vite. P2: dev create/join UI + netcode (prediction/interpolation/reconnect) rendering 4 haulers on box_level.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src test",
    "test": "vitest run"
  },
  "dependencies": {
    "@colyseus/sdk": "^0.17.43",
    "@dhaul/protocol": "workspace:*",
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
