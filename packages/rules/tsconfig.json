{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // Pure TS: ES2022 language lib only — no DOM, no Node types (C07-T03).
    "lib": ["ES2022"],
    "types": [],
    "outDir": "dist",
    "rootDir": "src"
  },
  "include": ["src"]
}
