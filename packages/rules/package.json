{
  "name": "@dhaul/rules",
  "version": "0.1.0",
  "private": true,
  "description": "Dungeon Haul pure rules engine — treasure value + share modifiers (C-07). No Phaser, no Colyseus, no Node I/O.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src test",
    "test": "vitest run"
  }
}
